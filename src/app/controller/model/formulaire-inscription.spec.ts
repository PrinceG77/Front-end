import { FormulaireInscription } from './formulaire-inscription';

describe('FormulaireInscription', () => {
  it('should create an instance', () => {
    expect(new FormulaireInscription()).toBeTruthy();
  });
});
